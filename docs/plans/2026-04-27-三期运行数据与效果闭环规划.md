# 2026-04-27 三期运行数据与效果闭环规划

## 0. 背景与输入

本规划基于以下已落盘结论与线索：

- 二期收口结论：`docs/二期最终收口记录-20260424.md`
- 二期 QA 收口：`docs/QA二期收口测试报告-20260424.md`
- 二期多分类收口：`docs/公司提示词库多分类管理-最终收口-20260425.md`
- 近期本地稳定化：`docs/QA收尾报告-2026-04-27-local-debug-3010.md`
- 三期方向基线：`docs/04-技术方案与架构设计.md`

---

## 1. 本次修订结论（范围收窄）

三期当前仅聚焦两件事：

1. 上线可用的 **1-5 分打分能力**（最小闭环）。
2. 关闭二期遗留的工程治理问题（5 项必做）。

以下能力明确不纳入本次三期，仅作为远期候选：**A/B 实验、Langfuse 对接、复杂效果面板**。

---

## 2. 三期目标

三期总体目标：在工程可控前提下，先完成“可评分、可统计、可追溯”的最小效果闭环，并补齐二期治理缺口。

1. **打分能力（1-5 分）**
- 支持对 Prompt 运行结果进行人工评分，评分范围固定为 `1~5`（整数）。
- 每条评分必须关联：`prompt`、`version`、`scene`；可选关联：`trace_id`。
- 形成最小统计结果，仅包括：
- 平均分（Average Score）
- 评分分布（1/2/3/4/5 各档占比或计数）
- 低分率（Low Score Rate，默认定义为 `score <= 2` 占比）

2. **二期遗留问题关闭**
- `build:web` 红灯治理
- CI 门禁不足治理
- 接口契约文档缺失治理
- 端口/容器竞争治理
- 首页创建/导入入口提示治理

---

## 3. 范围边界

### 3.1 三期纳入范围（In Scope）

- 打分数据模型与写入链路（1-5 分）。
- 打分关联维度：`prompt/version/scene`，可选 `trace_id`。
- 基础统计查询与展示能力（仅平均分、评分分布、低分率）。
- 二期遗留治理 5 项（见第 5 章）。

### 3.2 三期不纳入范围（Out of Scope）

- A/B 实验能力（实验配置、分流、归因、实验复盘）。
- Langfuse 对接（事件上报、回流同步、对账机制）。
- 复杂效果面板（多维高级筛选、版本/实验对比、高级分析图表）。
- 多租户重构、数据湖/流计算、自动调参等重型建设。

### 3.3 远期候选（Backlog）

- A/B 实验闭环。
- Langfuse（或同类平台）对接。
- 复杂效果面板与高级分析。

---

## 4. 打分能力设计（本期最小闭环）

### 4.1 打分记录模型（最小字段）

- `score`：整数，取值 `1~5`（必填）。
- `prompt_id`：关联 Prompt（必填）。
- `prompt_version_id`：关联版本（必填）。
- `scene`：调用场景（必填）。
- `trace_id`：调用追踪 ID（可选）。
- `created_at` / `created_by`：审计字段（必填）。

### 4.2 打分链路要求

- 评分写入需进行范围校验：非 `1~5` 直接拒绝。
- 评分写入失败必须可见（日志或错误提示），避免静默丢失。
- 打分记录需支持按 `prompt/version/scene` 过滤查询。

### 4.3 基础统计口径（仅三项）

- 平均分：按筛选条件计算算术平均。
- 评分分布：1/2/3/4/5 档计数与占比。
- 低分率：`score <= 2` 的评分数 / 总评分数。
- 本期不引入成功率、错误率、P95、实验对比等扩展指标。

---

## 5. 二期遗留治理项（三期必做）

### G0-1 `build:web` 红灯治理

- 目标：消除历史“偶发红灯/环境相关红灯”，形成可重复绿灯基线。
- 交付：构建前清理策略、稳定构建脚本、失败归因指引。
- 验收：连续 10 次本地+CI 构建通过率 100%。

### G0-2 CI 门禁不足治理

- 目标：把“发布相关主链路”纳入 PR 必经门禁，而非局部或手工触发。
- 交付：统一 `phase3-core-verify` 工作流（`build:web`、关键 integration、关键 e2e、契约校验）。
- 验收：PR 无法绕过门禁直接合并；任一关键链路回归可被阻断。

### G0-3 接口契约文档缺失治理

- 目标：形成可版本化、可审计的 API 契约资产（含错误码/示例）。
- 交付：`docs/contracts/` 目录与首批契约文档（评分写入、评分查询、基础统计）。
- 验收：前后端/测试按契约独立实现，抽样接口一致性检查通过。

### G0-4 端口/容器竞争治理

- 目标：解决并发测试与本地调试中的端口/容器名竞争。
- 交付：容器命名隔离策略、并发测试编排规则、冲突检测与 fail-fast 提示。
- 验收：并发执行关键测试集 5 轮无容器名冲突与端口占用失败。

### G0-5 首页创建/导入入口提示治理

- 目标：统一首页到管理创建/导入链路的提示与引导，避免“能点但认知不清”。
- 交付：首页入口文案与空态提示规范、跳转后状态反馈规范。
- 验收：新用户走查可在 1 分钟内完成“从首页进入创建/导入并看到明确反馈”。

---

## 6. 实施阶段（收敛版）

### M0（第 1-2 周）：治理关口（G0）

- 完成第 5 章 5 项治理。
- 产出：治理报告、门禁工作流、契约文档初版、并发稳定性报告。

### M1（第 3-4 周）：评分能力上线

- 交付 1-5 分评分写入能力与校验。
- 完成评分与 `prompt/version/scene` 的关联，支持可选 `trace_id`。
- 产出：评分能力说明、联调与回归报告。

### M2（第 5 周）：基础统计收口与验收

- 交付基础统计（平均分、评分分布、低分率）。
- 完成本期验收与文档归档。
- 产出：指标口径文档、验收记录。

---

## 7. 验收标准

### 7.1 阶段验收

- M0：5 项治理全部关闭，CI 门禁可阻断，契约文档可用。
- M1：评分写入成功率 >= 99%，非法评分（非 1-5）被 100% 拒绝。
- M2：基础统计三项指标可按 `prompt/version/scene` 查询，结果可复算。

### 7.2 全量 Done Definition

- 三期当前范围（评分能力 + 二期遗留治理）均有文档、自动化验证与可演示链路。
- 构建与关键测试门禁稳定，未回退到人工验收主导。
- 关键报告全部落盘 `docs/`（含命令与结果摘要）。
- A/B、Langfuse、复杂效果面板已在 Backlog 明确记录，不作为本期阻塞项。

---

## 8. 风险与依赖

### 8.1 主要风险

1. 评分口径漂移：不同角色对 1-5 分标准理解不一致。  
2. 低分率阈值争议：默认 `<=2` 需在验收前固定。  
3. 工程债回流：若治理不彻底，评分能力上线仍会被环境问题拖慢。  

### 8.2 关键依赖

- 研发：前后端共同维护评分与统计契约。
- QA：建立评分写入与统计口径专项回归。
- 运维/平台：CI 资源与容器编排稳定支持。
- 产品/运营：定义 1-5 分评分标准与低分处理策略。

---

## 9. 建议优先级

1. **P0（必须）**：G0 前置治理 5 项（M0）。  
2. **P0（必须）**：1-5 分评分能力与三项基础统计（M1-M2）。  
3. **P2（远期候选）**：A/B、Langfuse、复杂效果面板（Backlog）。  

建议执行顺序：**先治稳（G0）-> 再评分（M1）-> 再基础统计收口（M2）**。
