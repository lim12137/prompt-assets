# 并发测试报告 - 2026-04-30 - 登录后右上姓名部门显示

## 任务
- 登录完成后，右上角显示 `姓名 / 部门`（无部门时仅显示姓名）。
- token/session 与 `/api/me` 返回需包含部门字段。
- OA 解析增加部门兼容字段回退：`department/dept/org/部门/组织/organization`。

## RED（先失败）

1. `node --test --experimental-strip-types tests/unit/auth/oa-client.test.ts`
   - 结果：失败（2/2 fail）
   - 摘要：
     - 旧 OA 解析把 `name` 解析成 `Alice","department":"安全部"}`（正则误吞）
     - `department` 字段未被解析（`undefined !== 研发部`）

2. `node --test --experimental-strip-types tests/integration/api/login-auth.test.ts`
   - 结果：失败（1/3 fail）
   - 摘要：
     - `/api/me` 返回中 `user.department` 缺失（`undefined !== 安全部`）

3. `pnpm exec playwright test tests/e2e/smoke/global-auth-header.spec.ts`
   - 结果：失败（1/2 fail）
   - 摘要：
     - 右上角仅显示 `E2E用户`，未显示 `E2E用户 / 安全部`

## GREEN（修复后）

1. `node --test --experimental-strip-types tests/unit/auth/oa-client.test.ts`
   - 结果：通过（2/2 pass）

2. `node --test --experimental-strip-types tests/unit/auth/session.test.ts`
   - 结果：通过（7/7 pass）

3. `node --test --experimental-strip-types tests/integration/api/login-auth.test.ts`
   - 结果：通过（3/3 pass）

4. `pnpm exec playwright test tests/e2e/smoke/global-auth-header.spec.ts`
   - 结果：通过（2/2 pass）

## 页面验证

1. 本机服务可达性检查：
   - 命令：`Invoke-WebRequest -Uri http://127.0.0.1:3013 -UseBasicParsing`
   - 结果：`200`

2. 右上角展示验证：
   - 通过 e2e 用例 `global-auth-header.spec.ts` 验证已登录态文案包含 `E2E用户 / 安全部`。

## 变更说明

- `apps/web/lib/auth/oa-client.ts`
  - 增加 JSON 优先 + 正则兜底解析，兼容部门字段别名。
- `apps/web/lib/auth/session.ts`
  - `LoginTokenUser` 增加可选 `department`，并在验签时校验类型。
- `apps/web/app/api/login/route.ts`
  - 登录签发 token 与返回 `user` 均携带 `department`。
- `apps/web/app/_shared/auth-status.js`
  - 已登录显示改为：有部门则 `姓名 / 部门`，无部门仅姓名。
- 测试同步更新：
  - `tests/unit/auth/oa-client.test.ts`
  - `tests/unit/auth/session.test.ts`
  - `tests/integration/api/login-auth.test.ts`
  - `tests/integration/api/_auth-test-helpers.ts`
  - `tests/e2e/smoke/global-auth-header.spec.ts`
