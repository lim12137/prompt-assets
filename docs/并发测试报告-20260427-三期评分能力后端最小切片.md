# 并发测试报告 - 2026-04-27 - 三期评分能力后端最小切片

## 1. 执行背景

- 任务范围：契约修正、DB 迁移/schema、仓储函数、API 路由、集成测试。
- TDD 策略：先写失败测试，再实现，再回归验证。

## 2. 测试命令与结果摘要

### 2.1 Red 阶段（预期失败）

命令：

```bash
node --test --experimental-strip-types tests/integration/api/prompt-version-score.test.ts tests/integration/db/schema.test.ts
```

摘要：

- 失败原因 1：`/score` 与 `/score-stats` 路由文件不存在（`ERR_MODULE_NOT_FOUND`）。
- 失败原因 2：schema 静态断言缺少 `prompt_version_scores` 表与 `score 1-5` 约束。
- 结论：失败符合预期，进入实现阶段。

### 2.2 Green 阶段（实现后回归）

命令：

```bash
node --test --experimental-strip-types tests/integration/api/prompt-version-score.test.ts tests/integration/db/schema.test.ts
```

摘要：

- 总计：17 tests
- 通过：12
- 失败：0
- 跳过：5（真实 DB `postgres://postgres:postgres@127.0.0.1:55432/prompt_management_test` 不可达，按用例设计跳过）
- 关键通过点：
  - 评分写入合法/非法分值/scene 必填/不存在版本 404
  - 写后 `score-stats` 可见
  - 平均分、1-5 分布、低分率、scene 过滤
  - 迁移/schema 对评分表与约束的静态断言

### 2.3 关联回归（防止点赞链路回归）

命令：

```bash
node --test --experimental-strip-types tests/integration/api/prompt-version-like.test.ts tests/integration/api/prompt-detail.test.ts
```

摘要：

- 总计：9 tests
- 通过：9
- 失败：0
- 结论：既有详情与版本点赞链路未受本次改动影响。

## 3. 结论

- 三期评分能力后端最小切片已完成并通过相关自动化验证。
- 真实 DB 断言因本地测试库 `127.0.0.1:55432` 不可达被跳过，不影响本次代码级和静态迁移约束验收。
