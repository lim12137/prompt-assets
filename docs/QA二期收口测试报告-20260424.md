# QA 二期收口测试报告（2026-04-24）

## 执行范围
1. 管理员创建 API/页面  
2. 管理员导入 API/页面  
3. 员工候选迭代与详情聚合卡片  
4. 官方/候选卡复制  
5. 真实 Docker DB 下管理链路与详情链路  
6. 构建

## 环境说明
- 仓库：`D:\1work\提示词管理`
- 执行日期：2026-04-24
- 本次仅验证，不修改业务代码

## 测试命令与结果

### A. 管理员创建 API/页面
```powershell
pnpm exec node --test --experimental-strip-types tests/integration/api/prompt-create.test.ts
pnpm exec playwright test tests/e2e/admin/create-import-flow.spec.ts
```
结果：
- `prompt-create.test.ts`：`5 passed`
- `create-import-flow.spec.ts`：`3 passed`

### B. 管理员导入 API/页面
```powershell
pnpm exec node --test --experimental-strip-types tests/integration/api/admin-prompts-import.test.ts
pnpm exec playwright test tests/e2e/admin/create-import-flow.spec.ts
```
结果：
- `admin-prompts-import.test.ts`：`7 passed`
- 导入页面交互已包含在 `create-import-flow.spec.ts`（通过）

### C. 员工候选迭代与详情聚合卡片
```powershell
pnpm exec node --test --experimental-strip-types tests/integration/api/prompt-submission.test.ts
pnpm exec node --test --experimental-strip-types tests/integration/api/prompt-detail-candidate-cards.test.ts
pnpm exec playwright test tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
```
结果：
- `prompt-submission.test.ts`：`8 passed`
- `prompt-detail-candidate-cards.test.ts`：`1 passed`
- `prompt-detail-candidate-flow.spec.ts`：`1 passed`

### D. 官方/候选卡复制
```powershell
pnpm exec playwright test tests/e2e/smoke/prompt-detail-copy.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
```
结果：
- `2 passed`

### E. 真实 Docker DB 链路（管理 + 详情）
```powershell
pnpm test:e2e:admin:db
pnpm test:e2e:detail:db
```
结果：
- `test:e2e:admin:db`：通过，`management-flow.spec.ts` `2 passed`
- `test:e2e:detail:db`：通过，`prompt-detail-real-db.spec.ts` `1 passed`

备注：
- 并发执行两条 DB 链路时曾出现容器名冲突/端口竞争（执行层并发问题），改为串行后稳定通过。

### F. 构建
```powershell
pnpm build:web
```
结果：
- 构建成功（Next.js 15.5.15），`Compiled successfully`，路由生成完成。

## 汇总结论
- 本次 QA 覆盖项全部通过（串行真实 DB 验证也通过）。
- 二期收口主路径在当前环境可用：管理员创建/导入、员工候选提交、详情聚合卡片展示、官方/候选卡复制、真实 DB 管理与详情链路、构建均正常。

## 主要风险
1. 真实 DB 测试脚本在并发运行时存在容器名/端口竞争风险（建议 CI 中串行执行或隔离容器名）。
2. Node Type-Stripping 与 module type 警告持续存在（不影响当前结果，但会影响日志可读性与未来兼容性）。
3. 工作区存在未纳管本地文件（如 `.env`、部分 docs plans），与本次验证无关，但建议保持隔离以减少噪声。

## 验收基线判定
- **达到验收基线（是）**。  
