# 管理流 E2E 稳定化测试记录（2026-04-24）

## 目标
- 将 `tests/e2e/admin/management-flow.spec.ts` 从不稳定的 mock/固定行断言改为基于真实 Docker DB 的稳定测试。
- 覆盖首页入口、管理页、approve/reject 关键交互。

## TDD 过程

### Red（先失败）
1. 准备真实测试库：
```powershell
pnpm db:test:prepare
```
结果摘要：成功启动 Docker 测试库，完成迁移与 seed（`pendingSubmissions: 2`）。

2. 运行改造后的 `management-flow.spec.ts`（保留旧“拒绝失败”预期）：
```powershell
$env:DATABASE_URL='postgres://postgres:postgres@127.0.0.1:55432/prompt_management_test'; $env:PROMPT_REPOSITORY_DATA_SOURCE='auto'; pnpm exec playwright test tests/e2e/admin/management-flow.spec.ts
```
结果摘要：`1 failed, 1 passed`。失败点为 reject 实际返回成功反馈（`已拒绝 ...`），与旧失败预期不一致。

### Green（最小修复）
1. 最小修复：将 reject 断言改为成功路径；保留并强化关键交互断言（处理中、列表数量变化、reload 持久性）。

2. 重新准备数据库并回归：
```powershell
pnpm db:test:prepare
$env:DATABASE_URL='postgres://postgres:postgres@127.0.0.1:55432/prompt_management_test'; $env:PROMPT_REPOSITORY_DATA_SOURCE='auto'; pnpm exec playwright test tests/e2e/admin/management-flow.spec.ts
```
结果摘要：`2 passed`。

3. 额外验证真实 DB 链路脚本：
```powershell
pnpm test:e2e:admin:db
```
结果摘要：`1 passed`（`management-real-db.spec.ts` 通过），并自动清理测试数据库容器。

## 结论
- `management-flow.spec.ts` 已切换为真实 DB 驱动，去除 route mock，不再依赖 fixture 漂移。
- 已避免写死 `submission-row-1/2` 与固定标题，改为动态读取待审核行及标题文本进行断言。
- 首页入口、管理页、approve/reject 关键交互覆盖保留并增强。
