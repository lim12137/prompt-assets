# 并发测试报告-20260430-匿名点赞规则更新

## 变更目标
- 版本卡片点赞：不再要求登录，仅按 IP 做同日限频。
- 候选迭代提交：保持未登录 `401`。
- 评分：保持现状（需登录 + IP 日频限制）。

## RED（先失败）
### 命令
```powershell
$env:TEST_DB_PORT='55433'; $env:TEST_DATABASE_URL='postgres://postgres:postgres@127.0.0.1:55433/prompt_management_test'; node --test --experimental-strip-types tests/unit/auth/request-ip.test.ts tests/integration/api/prompt-version-like.test.ts tests/integration/api/prompt-version-score.test.ts tests/integration/api/prompt-submission.test.ts
```

### 结果摘要
- `tests/integration/api/prompt-version-like.test.ts` 新增/修改的未登录点赞用例失败：
  - 期望 `200`，实际 `401`（2 个失败用例）
- 其余相关测试通过。

## GREEN（改实现后通过）
### 命令
```powershell
$env:TEST_DB_PORT='55433'; $env:TEST_DATABASE_URL='postgres://postgres:postgres@127.0.0.1:55433/prompt_management_test'; node --test --experimental-strip-types tests/unit/auth/request-ip.test.ts tests/integration/api/prompt-version-like.test.ts tests/integration/api/prompt-version-score.test.ts tests/integration/api/prompt-submission.test.ts
```

### 结果摘要
- `pass=33, fail=0, skip=0`
- 覆盖确认：
  - 未登录可对 version like 点赞成功（`200`）
  - 同 IP 同日同卡片第二次点赞返回 `429`
  - 候选提交未登录仍返回 `401`
  - 评分链路测试保持通过

## HTTP/QA 验证（3013）
### 验证脚本
```powershell
@'
const base='http://127.0.0.1:3013';
const headers={'x-forwarded-for':'203.0.113.200'};
const first=await fetch(`${base}/api/prompts/api-debug-assistant/versions/v0002/like`,{method:'POST',headers});
const second=await fetch(`${base}/api/prompts/api-debug-assistant/versions/v0002/like`,{method:'POST',headers});
const sub=await fetch(`${base}/api/prompts/api-debug-assistant/submissions`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({content:'anon candidate'})});
console.log(first.status, second.status, sub.status);
'@ | node -
```

### 结果摘要
- 未登录点赞第一次：`200`
- 未登录点赞第二次（同 IP、同日、同卡片）：`429`
- 未登录候选提交：`401`

## 备注
- 页面使用的是 version-like 路由；本次未变更 prompt-level like 规则。
- DELETE 取消点赞仍要求登录，未影响“未登录可点赞”主路径。
