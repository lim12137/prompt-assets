# Task9 TDD 测试报告（首页分类/搜索联动与空态）

## 执行时间
- 2026-04-23

## 测试命令
- `pnpm test:e2e`

## 失败到通过摘要
- 第一次（红灯）：`tests/e2e/smoke/home.spec.ts` 共 3 条，`task8` 通过，`task9` 两条失败。
- 失败点：
  - 不存在可点击的分类按钮 `编程`。
  - 不存在可交互的 `关键词搜索` 输入框。
- 按 TDD 增补断言：在 `task9: 关键词过滤与空态提示` 中新增 `清空搜索` 后恢复结果的断言。
- 第二次（红灯确认）：新增断言后继续失败，失败原因仍为缺少分类/搜索交互控件。
- 实现最小交互后第三次（绿灯）：`home.spec.ts` 3/3 全部通过。

## 本次覆盖点（Task9）
- 分类筛选：点击分类后只展示命中项。
- 关键词过滤：按标题/摘要关键词过滤列表。
- 空态提示：无结果时展示 `home-empty-state` 与提示文案。
- 清空搜索：点击 `清空搜索` 恢复结果。
