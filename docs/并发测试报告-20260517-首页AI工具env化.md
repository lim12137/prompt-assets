# 并发测试报告 - 2026-05-17 - 首页 AI 工具 env 配置化

## 1) 现有结构核对（smoke + env）

- 已核对首页 AI 工具 smoke：
  - `tests/e2e/smoke/home-ai-tools.spec.ts`
- 已核对 env 相关单测：
  - `tests/unit/env/env.test.ts`
  - `apps/web/lib/home-ai-tools-env.test.ts`（实现侧已有的 AI 工具 env 解析单测）
- 已确认首页渲染链路：
  - `apps/web/lib/env.ts` 解析 `AI_TOOLS` 与 `AI_TOOL_<SLUG>_*`
  - `apps/web/app/page.jsx` 将 `homeAiTools` 传入首页
  - `apps/web/app/_home/home-page-shell.jsx` -> `HomeAiToolsSection`

## 2) 本次新增测试（最小必要）

- 新增：`tests/e2e/smoke/home-ai-tools-env.spec.ts`
- 覆盖点：
  - 在 env 覆盖 `AI_TOOLS` 与 `AI_TOOL_CLAUDE_*` 的前提下，首页 AI 工具区仍渲染 3 个工具；
  - 3 个工具名称可见（`CEIC Chat`、`ChatGPT`、`Claude Pro`）；
  - 工具卡片数量为 3；
  - 被 env 覆盖的链接值生效（`Claude Pro -> https://claude.ai/pro`）。

## 3) 执行命令与结果摘要

1. 命令

```powershell
$env:AI_TOOLS='claude,ceic-chat,chatgpt'; $env:AI_TOOL_CLAUDE_NAME='Claude Pro'; $env:AI_TOOL_CLAUDE_URL='https://claude.ai/pro'; $env:AI_TOOL_CLAUDE_DESC='长文本与分析增强版'; pnpm exec playwright test tests/e2e/smoke/home-ai-tools-env.spec.ts
```

结果摘要：
- 通过：`1 passed`。
- 说明：env 覆盖下首页 AI 工具 3 卡片渲染正常。

2. 命令

```powershell
pnpm exec playwright test tests/e2e/smoke/home-ai-tools.spec.ts
```

结果摘要：
- 通过：`1 passed`。
- 说明：原有首页 AI 工具 smoke 仍通过，无回归。

3. 命令

```powershell
node --test --experimental-strip-types apps/web/lib/home-ai-tools-env.test.ts
```

结果摘要：
- 通过：`5 passed, 0 failed`。
- 说明：实现侧 AI 工具 env 解析测试通过。

4. 命令

```powershell
node --test --experimental-strip-types tests/unit/env/env.test.ts
```

结果摘要：
- 通过：`3 passed, 0 failed`。
- 说明：通用 env 单测未受影响。

## 4) 阻塞记录

- 无实现阻塞。
- 未出现“实现未完成导致失败”的情况。
