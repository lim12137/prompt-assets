# 并发测试报告-20260514-详情页版本点赞后端去重查询

## 任务背景
- 目标：优化 `POST /api/prompts/[slug]/versions/[versionNo]/like` 同一请求内重复目标定位查询。
- 已知重复点：`markPromptVersionDailyInteraction(...)` 与 `likePromptVersion(...)` 各自调用一次 `findPublishedPromptVersionLikeTarget(...)`。
- 约束：最小改动、保持业务语义不变。

## 测试命令
```powershell
$env:TEST_DATABASE_URL='postgresql://app_user:ChangeMe_2026_Strong!@10.45.131.70:55432/prompt_management_test'
$env:DATABASE_URL=$env:TEST_DATABASE_URL
node --test --experimental-strip-types tests/integration/api/prompt-version-like.test.ts
```

```powershell
$env:TEST_DATABASE_URL='postgresql://app_user:ChangeMe_2026_Strong!@10.45.131.70:55432/prompt_management_test'
$env:DATABASE_URL=$env:TEST_DATABASE_URL
node --test --experimental-strip-types tests/integration/api/prompt-version-score.test.ts
```

## 结果摘要
- `prompt-version-like`：13/13 通过。
- 新增用例 `POST /like 单次请求在 DB 路径只定位一次目标版本` 通过，断言计数为 `1`。
- `prompt-version-score`：9/9 通过，验证 `markPromptVersionDailyInteraction` 返回结构调整未引入评分链路回归。

## 结论
- 在真实 DB 路径下，单次点赞请求目标版本定位查询由“2 次”降为“1 次”。
- 点赞/限流/缺基础设施等既有语义保持不变，相关集成测试全绿。
