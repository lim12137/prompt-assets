# 并发测试报告-2026-05-14-详情页点赞乐观更新

## 背景
- 目标：优化详情页点赞“体感慢”，采用前端乐观更新，保留 pending 禁用态，并在失败时回滚。
- 范围：`apps/web/app/prompts/[slug]/_prompt-actions.js` 与详情页点赞 smoke 测试。

## 测试命令

```powershell
$env:PROMPT_REPOSITORY_DATA_SOURCE='fixture'; pnpm exec playwright test tests/e2e/smoke/prompt-detail-like.spec.ts
```

```powershell
$env:PROMPT_REPOSITORY_DATA_SOURCE='fixture'; pnpm exec playwright test tests/e2e/smoke/prompt-detail-like.spec.ts tests/e2e/smoke/prompt-detail-copy.spec.ts --workers=2
```

## 结果摘要
- 单文件点赞用例：`3 passed`
  - 覆盖“点击后未返回前立即显示点赞状态与计数变化”
  - 覆盖“请求失败后状态与计数回滚，并展示错误文案”
  - 覆盖“请求进行中按钮禁用（pending）”
- 并发回归（2 workers）：`4 passed`
  - `prompt-detail-like.spec.ts` 全通过
  - `prompt-detail-copy.spec.ts` 通过，确认本次改动未破坏详情页复制反馈链路

## 结论
- 本次最小前端优化已达到目标：用户点击后立即有正向反馈，失败可回滚，且保持处理中禁用态。
