# 后台管理链路修复测试报告（2026-04-24）

## 测试命令

```powershell
node --test --experimental-strip-types tests/integration/api/admin-submission-review.test.ts tests/integration/api/admin-submissions-list.test.ts
```

## 结果摘要

- 退出码：`0`
- 总计：`8` 通过，`0` 失败
- 覆盖重点：
  - `GET /api/admin/submissions` 可读取待审核列表（默认 `pending`）
  - 审核动作后列表可反映最新 pending 状态
  - 非 admin 访问管理接口被拒绝，并返回可机读错误码
  - approve/reject 在冲突、无权限、不存在等场景返回可机读错误码
