# 并发测试报告-模板

## 测试日期

- YYYY-MM-DD

## 测试环境

- 执行方式：`k6` / `docker run grafana/k6`
- 目标地址：`BASE_URL=...`
- 数据源模式：`PROMPT_REPOSITORY_DATA_SOURCE=...`
- 备注：如未实跑，请明确原因

## 测试场景

- 场景 1：`GET /api/prompts` 搜索并发
- 场景 2：`POST /api/prompts/{slug}/submissions` 投稿并发

## 测试命令

```powershell
# 按实际执行命令填写（可多条）
# 示例：
docker run --rm -i -e BASE_URL=http://host.docker.internal:3000 grafana/k6 run - < tests/concurrency/search-and-submit.js
```

## 并发参数

| 参数 | 取值 | 说明 |
| --- | --- | --- |
| `TEST_DURATION` |  | 总压测时长 |
| `SEARCH_VUS` |  | 搜索并发用户数 |
| `SUBMIT_VUS` |  | 投稿并发用户数 |
| `SEARCH_KEYWORD` |  | 搜索关键词 |
| `SEARCH_SORT` |  | 搜索排序参数 |
| `SUBMIT_SLUG` |  | 投稿目标提示词 slug |

## 结果摘要

- 搜索接口成功率：`%`
- 投稿接口成功率：`%`
- 失败请求数：`N`
- 是否出现重复版本号：`是/否`
- 关键阈值（如 `http_req_failed`、`p95`）是否达标：`是/否`

## 异常与问题

- 若无异常，填写：`无`
- 若有异常，至少记录：错误现象、影响范围、复现条件、临时结论

## 结论

- 判定：`通过/不通过/阻塞`
- 结论说明：一句话说明是否满足当前验收口径
