# 并发测试报告-20260429-QA-登录接入页面同步

## 测试命令

1. 浏览器脚本冒烟（直连运行中服务 `http://127.0.0.1:3011`）  
   `@playwright/test` + `chromium` 访问 `/login`、`/admin`、`/admin/create`、`/admin/import`、`/prompts/ux-research-plan`，采集跳转、控制台错误与请求头。

2. 定向 e2e 回归  
   `pnpm exec playwright test tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts --reporter=line`

## 结果摘要

- `/login?redirect=%2Fadmin%2Fcreate` 可打开，表单存在；错误登录不会白屏，页面显示错误文案（本地环境为 `AWS_PORTAL_URL is required`）。
- 未登录访问 `/admin`、`/admin/create`、`/admin/import` 均被重定向至 `/login?redirect=%2Fadmin`，未渲染后台数据。
- 提示词详情页点赞/评分/投稿相关前端请求已不再发送 `x-user-email` 请求头。
- `tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts` 通过（1 passed）。

## 代码变更

- `apps/web/app/prompts/[slug]/_prompt-actions.js`
  - 移除前端 `x-user-email` 注入。
  - 移除“员工邮箱”输入与前端身份伪造逻辑，改为完全依赖 cookie session。
- `tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts`
  - 更新断言：投稿请求不再携带 `x-user-email`。
  - 在未登录上下文下，投稿预期为失败（符合 session 鉴权模型）。
