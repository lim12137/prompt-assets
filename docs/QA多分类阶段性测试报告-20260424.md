# QA 多分类阶段性测试报告（2026-04-24）

## 1. 目标与范围

本轮仅做验证，不改代码。覆盖范围：

1. 公司提示词库首页标题 / 多选 OR / 待分类折叠。  
2. 管理端分类管理、多选创建、多选导入。  
3. 详情页多标签与候选链路基础不回归。  
4. 构建。

---

## 2. 执行环境

- 仓库：`D:\1work\提示词管理`
- 日期：`2026-04-24`
- 测试 DB：`postgres://postgres:postgres@127.0.0.1:55432/prompt_management_test`

---

## 3. 执行命令与结果

### 3.1 DB 准备

```bash
pnpm db:test:prepare
```

结果：`通过`  
摘要：Docker 测试库拉起成功，`0001_init.sql` 与 `0002_multi_category_baseline.sql` 均应用成功，seed 成功写入。

### 3.2 Integration（多分类核心 API）

```bash
node --test --experimental-strip-types tests/integration/api/prompts-list.test.ts tests/integration/api/admin-categories.test.ts tests/integration/api/prompt-detail.test.ts
```

结果：`通过`（16/16）  
摘要：
- `admin-categories` 全通过（列表/新增/预检查删除/确认删除/边界）。
- `prompts-list` 全通过（含多分类 OR、空 categories 不过滤）。
- `prompt-detail` 全通过（详情结构与正文可见性）。

### 3.3 E2E（首页 + 管理端 + 详情候选）

```bash
pnpm exec playwright test tests/e2e/smoke/home.spec.ts tests/e2e/admin/create-import-flow.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
```

结果：`失败`（7 passed, 1 failed）  
失败用例：
- `tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts`
- 失败点：找不到 `h2` 文案“官方推荐卡”。

通过摘要：
- 首页标题“公司提示词库”通过。
- 首页多选 OR 过滤通过。
- 首页待分类默认折叠且不可手动勾选通过。
- 首页多标签展示（卡片/列表）通过。
- 管理端分类管理新增/删除确认流程通过（mock API）。
- 管理端多选创建与多选导入前端校验通过（mock API）。

### 3.4 E2E（真实 DB 管理创建/导入）

```bash
pnpm test:e2e:admin:create-import:db
```

结果：`失败`  
失败用例：
- `tests/e2e/admin/create-import-real-db.spec.ts`
- 失败点：`getByLabel("内容")` 命中两个元素（`内容创作`复选框 + `内容`文本框），触发 strict mode violation。

### 3.5 E2E（真实 DB 详情候选链路）

```bash
pnpm test:e2e:detail:db
```

结果：`失败`  
失败用例：
- `tests/e2e/smoke/prompt-detail-real-db.spec.ts`
- 失败点：找不到 `h2` 文案“官方推荐卡”。

### 3.6 构建

```bash
pnpm build:web
```

结果：`通过`  
摘要：Next.js production build 成功，路由产物正常生成。

---

## 4. 结果汇总

- 通过：
  - DB 准备
  - Integration（16/16）
  - 构建
  - E2E：首页与管理端（mock）主验证
- 失败：
  - 详情页候选链路（非真实 DB、真实 DB 两条均失败）
  - 管理端真实 DB 创建/导入回归（标签定位冲突）

---

## 5. 风险与结论

当前阶段 **未达到验收基线**。  
阻断原因：

1. 详情页候选链路 e2e（含真实 DB）均无法通过，影响“详情页多标签与候选链路基础不回归”判定。  
2. 管理端真实 DB 创建/导入 e2e 存在定位冲突，导致“管理端真实链路”证据不成立。

建议在修复上述 3 个失败用例后，按本报告同一命令集复跑一次作为收口依据。

---

## 6. 第二次重跑结果（当日复测）

### 6.1 重跑命令清单

```bash
pnpm db:test:prepare
node --test --experimental-strip-types tests/integration/api/prompts-list.test.ts tests/integration/api/admin-categories.test.ts tests/integration/api/prompt-create.test.ts tests/integration/api/admin-prompts-import.test.ts tests/integration/api/prompt-detail.test.ts
node --test --experimental-strip-types tests/integration/api/admin-categories.test.ts
pnpm exec playwright test tests/e2e/smoke/home.spec.ts tests/e2e/admin/create-import-flow.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
pnpm test:e2e:admin:create-import:db
pnpm test:e2e:detail:db
pnpm test:e2e:admin:management-flow
pnpm build:web
```

### 6.2 重跑摘要

- `db:test:prepare`：通过。  
- integration 汇总：通过（其中首次并行触发 `admin-categories` 7 条 skip，随后单独补跑全通过 7/7）。  
- `home + admin(create-import-flow) + detail-candidate-flow`：7 通过，1 失败。  
  - 失败：`tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts`
  - 失败点：期望 `bob@example.com 的候选` 卡片数量为 1，实际为 0。  
- `test:e2e:admin:create-import:db`：失败。  
  - 失败：`tests/e2e/admin/create-import-real-db.spec.ts`
  - 失败点：导入页状态为 JSON 解析失败（测试构造 payload 解析报错），未进入“导入请求提交中”。  
- `test:e2e:detail:db`：通过（1/1）。  
- `test:e2e:admin:management-flow`：通过（2/2）。  
- `build:web`：通过。

### 6.3 复测后结论

与首次相比，真实 DB 详情链路已恢复通过，真实 DB 管理审核入口通过。  
当前仍有 2 个阻断失败（`prompt-detail-candidate-flow.spec.ts`、`create-import-real-db.spec.ts`），因此 **仍未达到阶段验收基线**。

---

## 7. 第三次重跑结果（当日再次复测）

### 7.1 重跑命令清单

```bash
pnpm db:test:prepare
node --test --experimental-strip-types tests/integration/api/prompts-list.test.ts tests/integration/api/admin-categories.test.ts tests/integration/api/prompt-create.test.ts tests/integration/api/admin-prompts-import.test.ts tests/integration/api/prompt-detail.test.ts
pnpm exec playwright test tests/e2e/smoke/home.spec.ts tests/e2e/admin/create-import-flow.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
pnpm test:e2e:admin:create-import:db
pnpm test:e2e:detail:db
pnpm test:e2e:admin:management-flow
pnpm build:web
```

### 7.2 重跑摘要

- `db:test:prepare`：通过。  
- integration 汇总：通过（32/32）。  
  - 覆盖 `prompts-list` / `admin-categories` / `prompt-create` / `admin-prompts-import` / `prompt-detail` 全部通过。  
- `home + admin(create-import-flow) + detail-candidate-flow`：通过（8/8）。  
  - 首页标题“公司提示词库”、多选 OR、待分类折叠不可勾选、多标签展示均通过。  
  - 管理端分类管理、多选创建/导入前端链路通过。  
  - 详情页官方推荐卡 + 员工候选卡 + 候选提交/复制链路通过。  
- `test:e2e:admin:create-import:db`：失败（环境阻断）。  
  - 失败阶段：`db:test:up`。  
  - 失败点：容器清理并发冲突（`removal of container prompt-management-test-db is already in progress`），未进入业务测试断言阶段。  
- `test:e2e:detail:db`：失败（环境阻断）。  
  - 失败阶段：`db:test:up`。  
  - 失败点：测试库就绪超时（`Test DB did not become ready within 30000ms`），未进入业务测试断言阶段。  
- `test:e2e:admin:management-flow`：通过（2/2，真实 DB）。  
- `build:web`：失败。  
  - 失败点：Next 类型产物缺失导致编译失败：`apps/web/.next/types/app/admin/create/page.ts not found`。  

### 7.3 复测后结论

本次相比上轮，非真实 DB 的 e2e 已全部恢复通过，真实 DB `admin:management-flow` 仍稳定通过。  
但由于：

1. 真实 DB 验收入口 `test:e2e:admin:create-import:db` 与 `test:e2e:detail:db` 在环境阶段失败，证据链不完整；  
2. `build:web` 当前失败；  

因此本轮 **未达到阶段验收基线**。

---

## 8. 第四次重跑结果（2026-04-25 复测，关注 `2a11747` / `f9374a3`）

### 8.1 重跑命令清单

```bash
pnpm db:test:prepare
node --test --experimental-strip-types tests/integration/api/prompts-list.test.ts tests/integration/api/admin-categories.test.ts tests/integration/api/prompt-create.test.ts tests/integration/api/admin-prompts-import.test.ts tests/integration/api/prompt-detail.test.ts
pnpm exec playwright test tests/e2e/smoke/home.spec.ts tests/e2e/admin/create-import-flow.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
pnpm test:e2e:admin:create-import:db
pnpm test:e2e:detail:db
pnpm test:e2e:admin:management-flow
pnpm build:web
```

### 8.2 重跑摘要

- `pnpm db:test:prepare`：失败（超时，1200s，无有效输出）。  
- integration 汇总：`25 pass / 7 skip / 0 fail`。  
  - `admin-categories` 全部被跳过，原因为测试库不可达（`postgres://postgres:postgres@127.0.0.1:55432/prompt_management_test`）。  
  - `prompts-list` / `prompt-create` / `admin-prompts-import` / `prompt-detail` 其余用例通过。  
- `home + admin(create-import-flow) + detail-candidate-flow`：失败（`6 pass / 2 fail`）。  
  - 失败 1：`tests/e2e/smoke/home.spec.ts`（多选 OR 过滤）未找到 `JavaScript 代码审查助手`。  
  - 失败 2：`tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts` 未找到 `data-testid="official-card"`。  
  - 同时出现 Next dev 运行时异常：`Could not find ... segment-explorer-node.js#SegmentViewNode in the React Client Manifest`。  
- 真实 DB 入口：均未形成有效业务断言。  
  - `pnpm test:e2e:admin:create-import:db`：超时（600s）。  
  - `pnpm test:e2e:detail:db`：超时，且报 `等待真实 DB 锁超时`（`prompt-management-real-db-e2e.lock`）。  
  - `pnpm test:e2e:admin:management-flow`：超时（600s）。  
- `pnpm build:web`：失败。  
  - 失败点：`PageNotFoundError: Cannot find module for page: /_document`。  

### 8.3 对本轮关注点（`2a11747` / `f9374a3`）判断

本次复测未证明“工程阻断已消失”，反而仍存在并新增阻断信号：

1. 真实 DB 链路仍不可稳定执行（DB 准备超时、锁超时、真实 DB 入口超时）。  
2. 构建阻断仍在（由上一轮的 `.next/types` 缺失演化为当前 `/_document` 缺失）。  
3. 非真实 DB e2e 出现回退（由上一轮 8/8 通过变为本轮 6/8）。  

结论：当前 **仍未达到阶段验收基线**。

---

## 15. 第十一次重跑结果（2026-04-25 复测，关注 `bc452f4` / `5f44cbd`）

### 15.1 重跑命令清单

```bash
pnpm db:test:prepare
node --test --experimental-strip-types tests/integration/api/admin-categories.test.ts
node --test --experimental-strip-types tests/integration/api/prompts-list.test.ts tests/integration/api/prompt-create.test.ts tests/integration/api/admin-prompts-import.test.ts tests/integration/api/prompt-detail.test.ts tests/integration/api/prompt-detail-candidate-cards.test.ts
pnpm exec playwright test tests/e2e/smoke/home.spec.ts tests/e2e/admin/create-import-flow.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
pnpm exec playwright test tests/e2e/smoke/home.spec.ts tests/e2e/admin/create-import-flow.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
pnpm test:e2e:admin:create-import:db
pnpm test:e2e:detail:db
pnpm test:e2e:admin:category-management:db
pnpm build:web
```

### 15.2 重跑摘要

- 当前代码位点：`HEAD=bc452f4`，且 `bc452f4` / `5f44cbd` 均为当前分支祖先提交。  
- `pnpm db:test:prepare`：通过。  
  - Docker 起库、迁移（`0001_init.sql` / `0002_multi_category_baseline.sql`）和 seed 一次成功。  
- integration：全部通过（`34 pass / 0 fail / 0 skip`）。  
  - `admin-categories`：`8/8` 通过（含删除预检查 token 用例）。  
  - 其余（`prompts-list` / `prompt-create` / `admin-prompts-import` / `prompt-detail` / `prompt-detail-candidate-cards`）全部通过。  
- smoke e2e（首页多分类 + 管理端多选创建/导入 + 详情候选链路）：  
  - 首轮：`7 pass / 1 fail`，失败为 `home.spec.ts` 的 `newPage` 初始化超时（30s），非业务断言失败。  
  - 第二轮整组复跑：`8/8` 全通过。  
- real-db 三入口：全部通过。  
  - `test:e2e:admin:create-import:db`：`1/1` 通过。  
  - `test:e2e:detail:db`：`1/1` 通过。  
  - `test:e2e:admin:category-management:db`：`1/1` 通过。  
- `pnpm build:web`：通过。  
  - Next.js 生产构建完成，页面与 API 路由产物生成正常。  

### 15.3 对本轮关注点（`bc452f4` / `5f44cbd`）判断

相较前几轮，本轮在目标范围内已形成完整可复验的通过证据链：  
首页多分类、管理端分类管理与多选创建/导入、详情多标签与候选链路、真实 DB create/import/detail/category-management、admin-categories integration、构建均已通过（smoke 首轮超时后整组复跑全绿）。

结论：当前 **已达到阶段验收基线**。

---

## 14. 第十次重跑结果（2026-04-25 最后一轮复测）

### 14.1 重跑命令清单

```bash
pnpm db:test:prepare
node --test --experimental-strip-types tests/integration/api/admin-categories.test.ts
node --test --experimental-strip-types tests/integration/api/prompts-list.test.ts tests/integration/api/prompt-create.test.ts tests/integration/api/admin-prompts-import.test.ts tests/integration/api/prompt-detail.test.ts
pnpm exec playwright test tests/e2e/smoke/home.spec.ts tests/e2e/admin/create-import-flow.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
pnpm test:e2e:admin:create-import:db
pnpm test:e2e:detail:db
pnpm test:e2e:admin:management-flow
pnpm build:web
```

### 14.2 重跑摘要

- `pnpm db:test:prepare`：通过。  
- integration：  
  - `admin-categories`：`7 pass / 1 fail`。  
  - 失败用例：`DELETE /api/admin/categories/[slug] 预检查返回计数与确认 token`。  
  - 失败点：`Connection terminated unexpectedly`（seed/DB 连接中断）。  
  - 其余 integration（`prompts-list` / `prompt-create` / `admin-prompts-import` / `prompt-detail`）：全通过（`25/25`）。  
- smoke e2e（首页 + 管理创建导入 + 详情候选）：失败（`2 pass / 6 fail`）。  
  - 失败表现：首页与管理页关键元素不可见、`/` 访问返回 `500`、测试内创建 API 返回 `500/404`。  
  - 伴随 Next dev 产物异常：`.next-e2e` 下多项 `MODULE_NOT_FOUND/ENOENT`（`middleware-manifest.json`、`pages-manifest.json`、`app/page.js`、`vendor-chunks/*`）。  
- real-db 三入口：部分通过。  
  - `test:e2e:admin:create-import:db`：失败（创建状态从“提交中”落到“创建失败：请求失败”，未到“已创建”）。  
  - `test:e2e:detail:db`：通过（`1/1`）。  
  - `test:e2e:admin:management-flow`：通过（`2/2`）。  
- `pnpm build:web`：通过。  

### 14.3 最终结论

本轮“最后一次”复测未形成全链路稳定通过证据：  
`admin-categories` integration 仍失败，smoke e2e 与 `create-import:db` 出现回归性失败。  

结论：当前 **仍未达到阶段验收基线**。

---

## 13. 第九次重跑结果（2026-04-25 复测，关注 `3989072` / `d3cb92a`）

### 13.1 重跑命令清单

```bash
pnpm db:test:prepare
node --test --experimental-strip-types tests/integration/api/prompts-list.test.ts tests/integration/api/admin-categories.test.ts tests/integration/api/prompt-create.test.ts tests/integration/api/admin-prompts-import.test.ts tests/integration/api/prompt-detail.test.ts
pnpm exec playwright test tests/e2e/smoke/home.spec.ts tests/e2e/admin/create-import-flow.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
pnpm test:e2e:admin:create-import:db
pnpm test:e2e:detail:db
pnpm test:e2e:admin:management-flow
pnpm build:web
```

### 13.2 重跑摘要

- `pnpm db:test:prepare`：通过。  
  - Docker 起库、迁移、seed 一次成功。  
- integration 汇总：`31 pass / 1 fail / 0 skip`。  
  - 唯一失败：`admin-categories` 用例 `DELETE /api/admin/categories/[slug] 预检查返回计数与确认 token`。  
  - 失败点：测试内 `createPrompt` 返回 `404`（期望 `201`）。  
- `home + admin(create-import-flow) + detail-candidate-flow`：通过（`8/8`）。  
- real-db 三入口：全部通过。  
  - `test:e2e:admin:create-import:db`：`1/1` 通过。  
  - `test:e2e:detail:db`：`1/1` 通过。  
  - `test:e2e:admin:management-flow`：`2/2` 通过。  
- `pnpm build:web`：通过。  
  - 生产构建与路由产物正常。  

### 13.3 对本轮关注点（`3989072` / `d3cb92a`）判断

与上一轮相比，`db:test:prepare` 掉线问题已明显改善（本轮通过）；但 integration 仍有 1 条真实失败，说明阶段性 QA 仍未完全过线。

结论：当前 **仍未达到阶段验收基线**。

---

## 11. 第七次重跑结果（2026-04-25 复测，关注 `4253e79` / `e3180f4`）

### 11.1 重跑命令清单

```bash
pnpm db:test:prepare
node --test --experimental-strip-types tests/integration/api/prompts-list.test.ts tests/integration/api/admin-categories.test.ts tests/integration/api/prompt-create.test.ts tests/integration/api/admin-prompts-import.test.ts tests/integration/api/prompt-detail.test.ts
pnpm exec playwright test tests/e2e/smoke/home.spec.ts tests/e2e/admin/create-import-flow.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
pnpm test:e2e:admin:create-import:db
pnpm test:e2e:detail:db
pnpm test:e2e:admin:management-flow
pnpm build:web
```

### 11.2 重跑摘要

- `pnpm db:test:prepare`：失败。  
  - 失败点：`db:test:seed` 连续 3 次失败，出现 `Connection terminated unexpectedly` 与 `ECONNREFUSED 127.0.0.1:55432`。  
- integration 汇总：`30 pass / 1 fail / 1 skip`。  
  - 失败用例：`DELETE /api/admin/categories/[slug] 预检查返回计数与确认 token`。  
  - 失败点：`admin-categories` 链路出现 `Connection terminated unexpectedly`。  
  - 同组另有 1 条 skip（测试库不可达）。  
- `home + admin(create-import-flow) + detail-candidate-flow`：通过（`8/8`）。  
- real-db 三入口：全部通过。  
  - `test:e2e:admin:create-import:db`：`1/1` 通过。  
  - `test:e2e:detail:db`：`1/1` 通过。  
  - `test:e2e:admin:management-flow`：`2/2` 通过。  
- `pnpm build:web`：通过。  
  - 本轮构建命令已切为 `prebuild-clean.mjs + run-next.mjs build`，Next.js 生产构建与路由产物正常。  

### 11.3 对本轮关注点（`4253e79` / `e3180f4`）判断

两个剩余工程阻断中：

1. 构建阻断：本轮已消失（`build:web` 通过）。  
2. DB/integration 稳定性阻断：仍未完全消失（`db:test:prepare` 失败，integration 存在 1 条真实失败）。  

结论：当前 **仍未达到阶段验收基线**。

---

## 10. 第六次重跑结果（2026-04-25 复测，关注 `abc3a89`）

### 10.1 重跑命令清单

```bash
pnpm db:test:prepare
node --test --experimental-strip-types tests/integration/api/prompts-list.test.ts tests/integration/api/admin-categories.test.ts tests/integration/api/prompt-create.test.ts tests/integration/api/admin-prompts-import.test.ts tests/integration/api/prompt-detail.test.ts
pnpm exec playwright test tests/e2e/smoke/home.spec.ts tests/e2e/admin/create-import-flow.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
pnpm test:e2e:admin:create-import:db
pnpm test:e2e:detail:db
pnpm test:e2e:admin:management-flow
pnpm build:web

# 稳定性补充复跑（三条 real-db 入口二次验证）
pnpm test:e2e:admin:create-import:db
pnpm test:e2e:detail:db
pnpm test:e2e:admin:management-flow
```

### 10.2 重跑摘要

- `pnpm db:test:prepare`：通过。  
  - Docker 起库、迁移（`0001_init.sql` / `0002_multi_category_baseline.sql`）和 seed 正常。  
- integration 汇总：`25 pass / 7 skip / 0 fail`。  
  - `admin-categories` 7 条仍提示测试库不可达而跳过。  
  - `prompts-list` / `prompt-create` / `admin-prompts-import` / `prompt-detail` 通过。  
- `home + admin(create-import-flow) + detail-candidate-flow`：通过（`8/8`）。  
- 三条 real-db 入口（首轮）：全部通过。  
  - `test:e2e:admin:create-import:db`：`1/1` 通过。  
  - `test:e2e:detail:db`：`1/1` 通过。  
  - `test:e2e:admin:management-flow`：`2/2` 通过。  
- 三条 real-db 入口（补充二次复跑）：全部再次通过。  
  - `test:e2e:admin:create-import:db`：`1/1` 通过。  
  - `test:e2e:detail:db`：`1/1` 通过。  
  - `test:e2e:admin:management-flow`：`2/2` 通过。  
- `pnpm build:web`：失败。  
  - 失败点：`PageNotFoundError: Cannot find module for page: /_document`。  

### 10.3 对本轮关注点（`abc3a89`）判断

`abc3a89` 后，三条 real-db 验收入口在本轮及补充复跑均通过，稳定性信号显著改善。  
但本轮 `build:web` 仍失败，且 integration 中 `admin-categories` 仍有 7 条 skip，导致整体验收证据链仍不完整。

结论：当前 **仍未达到阶段验收基线**。

---

## 9. 第五次重跑结果（2026-04-25 复测，关注 `a2a04e5` / `2b863cb`）

### 9.1 重跑命令清单

```bash
pnpm db:test:prepare
node --test --experimental-strip-types tests/integration/api/prompts-list.test.ts tests/integration/api/admin-categories.test.ts tests/integration/api/prompt-create.test.ts tests/integration/api/admin-prompts-import.test.ts tests/integration/api/prompt-detail.test.ts
pnpm exec playwright test tests/e2e/smoke/home.spec.ts tests/e2e/admin/create-import-flow.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
pnpm test:e2e:admin:create-import:db
pnpm test:e2e:detail:db
pnpm test:e2e:admin:management-flow
pnpm build:web
```

### 9.2 重跑摘要

- `pnpm db:test:prepare`：通过。  
  - Docker 起库、迁移（`0001_init.sql` / `0002_multi_category_baseline.sql`）与 seed 成功。  
- integration 汇总：`25 pass / 7 skip / 0 fail`。  
  - `admin-categories` 7 条被跳过，提示测试库不可达（`postgres://postgres:postgres@127.0.0.1:55432/prompt_management_test`）。  
  - `prompts-list` / `prompt-create` / `admin-prompts-import` / `prompt-detail` 通过。  
- `home + admin(create-import-flow) + detail-candidate-flow`：通过（`8/8`）。  
  - 首页标题、多选 OR、待分类折叠不可勾选、多标签展示通过。  
  - 管理端分类管理、多选创建/导入通过。  
  - 详情页官方卡/候选卡、候选提交与复制链路通过。  
- `pnpm test:e2e:admin:create-import:db`：失败。  
  - 失败点：真实 DB e2e 启动前端口冲突（`http://localhost:3010 is already used`）。  
- `pnpm test:e2e:detail:db`：失败。  
  - 失败点：`db:test:migrate` 阶段失败，出现 `duplicate key ... pg_database_datname_index (prompt_management_test already exists)` 与 `ECONNREFUSED 127.0.0.1:55432`。  
- `pnpm test:e2e:admin:management-flow`：失败。  
  - 失败点：真实 DB 准备阶段不稳定，出现容器名冲突（`container name ... already in use`）与 seed 连接中断（`Connection terminated unexpectedly`）。  
- `pnpm build:web`：通过。  
  - Next.js 生产构建成功，路由产物正常生成。  

### 9.3 对本轮关注点（`a2a04e5` / `2b863cb`）判断

本轮相比上一轮，首页/管理端/详情 smoke e2e 与构建阻断已恢复；但真实 DB 三个验收入口仍未稳定通过，阻断集中在测试环境编排（端口、容器、迁移/seed 幂等性）阶段。

结论：当前 **仍未达到阶段验收基线**。

---

## 12. 第八次重跑结果（2026-04-25 复测，关注 `746e6b2`）

### 12.1 重跑命令清单

```bash
pnpm db:test:prepare
node --test --experimental-strip-types tests/integration/api/prompts-list.test.ts tests/integration/api/admin-categories.test.ts tests/integration/api/prompt-create.test.ts tests/integration/api/admin-prompts-import.test.ts tests/integration/api/prompt-detail.test.ts
pnpm exec playwright test tests/e2e/smoke/home.spec.ts tests/e2e/admin/create-import-flow.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
pnpm test:e2e:admin:create-import:db
pnpm test:e2e:detail:db
pnpm test:e2e:admin:management-flow
pnpm build:web
```

### 12.2 重跑摘要

- `pnpm db:test:prepare`：失败。  
  - 失败点：`db:test:seed` 与 `db:test:migrate` 多次重试仍失败，出现 `Connection terminated unexpectedly`、`duplicate key ... pg_database_datname_index (prompt_management_test already exists)`。  
- integration 汇总：`30 pass / 1 fail / 1 skip`。  
  - 失败用例：`DELETE /api/admin/categories/[slug] 预检查返回计数与确认 token`。  
  - 失败点：`admin-categories` 链路仍出现 `Connection terminated unexpectedly`（并伴随测试库不可达 skip）。  
- `home + admin(create-import-flow) + detail-candidate-flow`：失败（`5 pass / 3 fail`）。  
  - 失败点 1：`/admin/create` 页找不到 `待分类` 标签（`getByLabel("待分类")` not found）。  
  - 失败点 2：`home.spec.ts` 中创建 API 返回 `404`（期望 `201`）。  
  - 失败点 3：详情候选提交未出现“提交成功”。  
  - 伴随 Next 开发态错误：`Cannot find module for page: /api/prompts/[slug]/submissions/route` 以及 `vendor-chunks/drizzle-orm... MODULE_NOT_FOUND`。  
- real-db 三入口：部分通过。  
  - `test:e2e:admin:create-import:db`：失败（`getByLabel("编程").check()` 超时，元素未找到）。  
  - `test:e2e:detail:db`：通过（`1/1`）。  
  - `test:e2e:admin:management-flow`：通过（`2/2`）。  
- `pnpm build:web`：通过。  
  - `prebuild-clean + run-next build` 全流程成功，路由产物正常。  

### 12.3 对本轮关注点（`746e6b2`）判断

目标“`db:test:prepare` 与 `admin-categories` 不再掉线”本轮未达成：

1. `db:test:prepare` 仍不稳定失败；  
2. `admin-categories` 仍有连接中断导致的真实失败；  
3. 同时出现新的 smoke/real-db 交互回归（创建页标签与 submissions API 路由异常）。

结论：当前 **仍未达到阶段验收基线**。
