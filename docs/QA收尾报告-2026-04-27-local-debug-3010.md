# QA 收尾报告（2026-04-27，local-debug 3010）

## 范围
- 目标：对“本地服务恢复 + local-debug 3010 端口回收闭环”已通过结果做收尾归档。
- 工作目录：`D:\1work\提示词管理`

## 本次 QA/验收执行命令与结果摘要

1. 服务可用性与恢复验证（见 `docs/qa-2026-04-27-enospc-recovery.md`）

```powershell
Get-PSDrive -Name D
pnpm local:db:status
Start-Process -FilePath node -ArgumentList '.\scripts\local-debug.mjs','web' -WorkingDirectory 'D:\1work\提示词管理' -WindowStyle Hidden
Invoke-WebRequest http://127.0.0.1:3010/
Invoke-WebRequest http://127.0.0.1:3010/api/health
pnpm test:runtime
```

- 结果摘要：
  - `GET /` 返回 `200`
  - `GET /api/health` 返回 `200`，状态 `{"status":"ok"}`
  - `pnpm test:runtime`：`pass 2 / fail 0`（退出码 `0`）

2. 3010 端口自动回收与旧 PID -> 新 PID 闭环验收（见 `docs/测试记录-2026-04-26-local-debug-3010-旧PID新PID闭环.md`）

```powershell
node --test tests/unit/scripts/local-debug-port-ownership.test.mjs tests/unit/scripts/local-debug-reclaim-flow.test.mjs tests/unit/scripts/local-debug-safe-stop-policy.test.mjs tests/unit/scripts/local-debug.test.mjs
node ./scripts/local-debug.mjs stop-web
cmd /d /s /c "pnpm dev:web"
cmd /d /s /c "local-debug.bat web"
```

- 结果摘要：
  - 单测集合通过：`23 pass / 0 fail / 1 skip`
  - 真机端口闭环通过：旧 PID 被回收，新 PID 接管 `3010`
  - `http://127.0.0.1:3010` 返回 `200`

## 验收结论
- 本次收尾确认：服务实测正常；`/qa`（对应最小 smoke/runtime QA）已通过；结果已在 `docs` 落盘保存。
