# 并发测试报告-20260423-搜索与投稿

## 测试日期

- 2026-04-23

## 测试环境

- 仓库路径：`D:\1work\提示词管理`
- 运行方式：本次仅完成脚本与报告资产交付，未执行真实 k6 压测
- 约束口径：当前环境不要求真实跑 Docker Hub / k6

## 测试场景

- 场景 1：`20` 搜索并发，目标接口 `GET /api/prompts`
- 场景 2：`10` 投稿并发，目标接口 `POST /api/prompts/{slug}/submissions`

## 测试命令

```powershell
# 计划执行命令（本次未实跑）
docker run --rm -i `
  -e BASE_URL=http://host.docker.internal:3000 `
  -e SEARCH_VUS=20 `
  -e SUBMIT_VUS=10 `
  grafana/k6 run - < tests/concurrency/search-and-submit.js
```

## 并发参数

| 参数 | 值 | 状态 |
| --- | --- | --- |
| `TEST_DURATION` | `30s`（脚本默认） | 已定义 |
| `SEARCH_VUS` | `20`（脚本默认） | 已定义 |
| `SUBMIT_VUS` | `10`（脚本默认） | 已定义 |
| `SEARCH_KEYWORD` | `API`（脚本默认） | 已定义 |
| `SEARCH_SORT` | `latest`（脚本默认） | 已定义 |
| `SUBMIT_SLUG` | `api-debug-assistant`（脚本默认） | 已定义 |

## 数据准备情况

- 已准备：
  - 并发脚本：`tests/concurrency/search-and-submit.js`
  - 报告模板：`docs/并发测试报告-模板.md`
  - 目标接口与参数映射（搜索/query 参数、投稿 body 与 `x-user-email`）已在脚本中固化
- 未实跑：
  - k6 压测实际执行输出
  - 成功率、失败请求数、时延分位值
  - “是否出现重复版本号”的真实观测结果

## 结果摘要

- 搜索接口成功率：`N/A（未实跑）`
- 投稿接口成功率：`N/A（未实跑）`
- 失败请求数：`N/A（未实跑）`
- 是否出现重复版本号：`N/A（未实跑）`
- 阈值达标情况：`N/A（未实跑）`

## 异常与问题

- 当前阻塞项：
  - 本次任务口径明确“无需真实跑 Docker Hub/k6”，因此没有生成实测指标
  - 若要补全真实结果，需要可访问的目标服务（`BASE_URL`）与可用的 k6 执行环境（本机或 Docker）

## 结论

- 判定：`在 Task17 当前口径下通过`
- 说明：并发脚本骨架、报告模板、当日报告占位已落盘；真实压测指标留待后续实跑补录
