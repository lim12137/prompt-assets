# 并发测试报告 - 2026-04-29 - 评分/点赞 IP 日频限制与 QA 页面同步

## 目标
- 落实规则：同一 IP 对同一卡片（`prompt_version_id`）同日只能点赞一次、评分一次。
- 保持候选提交未登录 401，不回退。
- 页面同步 401/429/409 的提示文案。
- 验证 `/qa`（Playwright 关键冒烟）同步通过。

## RED（先失败）
### 命令
```bash
node --test --experimental-strip-types tests/unit/auth/request-ip.test.ts tests/integration/api/prompt-version-like.test.ts tests/integration/api/prompt-version-score.test.ts tests/integration/api/prompt-submission.test.ts
```

### 结果摘要
- 失败 3 项（符合 RED 预期）：
  - `prompt-version-like`: 同 IP 同日重复点赞期望 429，实际 200。
  - `prompt-version-score`: 同 IP 同日重复评分期望 429，实际 200。
  - `request-ip.test.ts`: helper 文件不存在。

## GREEN（实现后通过）
### 命令
```bash
node --test --experimental-strip-types tests/unit/auth/request-ip.test.ts tests/integration/api/prompt-version-like.test.ts tests/integration/api/prompt-version-score.test.ts tests/integration/api/prompt-submission.test.ts
```

### 结果摘要
- `30 passed, 0 failed`。
- 覆盖点：
  - `x-forwarded-for` / `x-real-ip` / `unknown` 提取。
  - Asia/Shanghai 跨日 key。
  - 点赞/评分同 IP 同日同卡片 429。
  - 不同 IP 同卡片同日可操作。
  - 候选提交未登录维持 401。

## /qa 页面同步验证
### 命令
```bash
pnpm test:e2e:critical
```

### 结果摘要
- `1 passed, 0 failed`。
- 用例：`tests/e2e/smoke/prompt-detail-ux-candidate-like.spec.ts`
  - 未登录点击候选版本点赞：
    - 接口返回 401；
    - 页面展示“请先登录后再操作”；
    - 点赞数不增加。

