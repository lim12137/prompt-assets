# 真实库样例清理执行记录

## 任务范围

- 任务：真实库初始样例清理
- 日期：2026-05-01
- 范围限制：
  - 未修改 `tests/fixtures/prompts.ts`
  - 未修改 `packages/db/src/seed.ts`
  - 未扩展到后台页面或前端

## 测试环境

- 仓库路径：`D:\1work\提示词管理`
- Shell：PowerShell
- Node 测试框架：`node --test`

## 测试命令

```powershell
node --test tests/unit/scripts/cleanup-real-db-sample-prompts.test.mjs
```

## 测试结果摘要

- 结果：通过
- 通过用例：4
- 失败用例：0

覆盖点：

1. `--dry-run` 只输出待删摘要，不执行删除
2. `--confirm` 缺少 `--operator` / `--reason` 时拒绝执行
3. `--confirm` 按事务顺序删除 `submissions -> current_version_id 置空 -> prompts 删除 -> audit`
4. 输入只接受显式 `slug` 清单

## 本次实现产物

- 脚本：`scripts/cleanup-real-db-sample-prompts.mjs`
- 测试：`tests/unit/scripts/cleanup-real-db-sample-prompts.test.mjs`
- 执行说明：`docs/2026-05-01-真实库样例清理执行说明.md`

## 真实库执行情况

### 实际清理记录

- 执行时间：2026-05-01
- 目标真实库：`postgres://postgres:postgres@127.0.0.1:55432/prompt_management`
- 输入文件：`.tmp/2026-05-01-initial-sample-cleanup-slugs.json`
- 删除原因：清理最初 10 条无用提示词样例

#### 预备修正

真实库 schema 与脚本默认假设存在轻微漂移：

- 真实库缺少 `prompt_version_likes` 表
- `prompt_versions` 使用 `version_no`，不是 `version_number`

为保证真实库可执行，本次先补了脚本兼容：

1. 当 `prompt_version_likes` 不存在时，`dry-run` 摘要按 `versionLikeCount = 0` 处理
2. 增加对应单元测试，覆盖旧 schema 下仍可预览

#### 执行命令

```powershell
node --test tests/unit/scripts/cleanup-real-db-sample-prompts.test.mjs
```

```powershell
$env:DATABASE_URL='postgres://postgres:postgres@127.0.0.1:55432/prompt_management'
node scripts/cleanup-real-db-sample-prompts.mjs --dry-run --input .tmp/2026-05-01-initial-sample-cleanup-slugs.json
```

```powershell
$env:DATABASE_URL='postgres://postgres:postgres@127.0.0.1:55432/prompt_management'
@'
(async () => {
  const repo = await import('./apps/web/lib/api/prompt-repository.ts');
  const slugs = [
    'business-data-analysis',
    'data-analyst',
    'data-scientist',
    'scientific-data-visualizer',
    'voice-input-optimizer',
    'meeting-summary',
    'note-taking-assistant',
    'writing-assistant',
    'content-summarizer',
    'human-resources-management',
    'administration',
    'project-management',
    'legal-affairs',
    'academic-researcher',
    'journalist',
    'writing-materials-collector'
  ];
  for (const slug of slugs) {
    const detail = await repo.getPromptDetail(slug);
    console.log(JSON.stringify({
      slug,
      ok: Boolean(detail),
      title: detail?.title ?? null,
      currentVersionNo: detail?.currentVersion?.versionNo ?? null
    }));
  }
})();
'@ | node --experimental-strip-types
```

#### 结果摘要

- 单测：`5 passed / 0 failed`
- `dry-run`：`foundCount = 0`，`missingSlugs` 包含全部 10 条目标 slug
- 结论：当前真实库里这 10 条初始样例已不存在，因此无需执行 `--confirm`
- Cherry 首批 16 条验收：
  - 数据库存在数：`16 / 16`
  - `getPromptDetail()` 可读数：`16 / 16`
  - 版本号：15 条为 `v0001`，`journalist` 为 `v0002`

#### 目标 10 条 dry-run 结果

- `blog-outline-generator`：不存在
- `social-hook-pack`：不存在
- `short-video-script`：不存在
- `newsletter-polisher`：不存在
- `js-code-reviewer`：不存在
- `sql-index-advisor`：不存在
- `api-debug-assistant`：不存在
- `landing-copy-framework`：不存在
- `figma-wireframe-brief`：不存在
- `ux-research-plan`：不存在

#### 备注

- 因 `dry-run` 已确认 10 条目标在真实库中不存在，本次没有执行 `--confirm`，也没有新增清理审计日志。
- 本地 `http://127.0.0.1:3010/api/prompts/{slug}` 在本次会话中出现 20 秒超时，属于当前服务态问题；但直接调用仓库 `getPromptDetail()` 对真实库验收通过，说明数据仍完整可读。
