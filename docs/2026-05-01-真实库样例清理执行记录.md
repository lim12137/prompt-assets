# 真实库样例清理执行记录

## 任务范围

- 任务：真实库初始样例清理
- 日期：2026-05-01
- 范围限制：
  - 未修改 `tests/fixtures/prompts.ts`
  - 未修改 `packages/db/src/seed.ts`
  - 未扩展到后台页面或前端

## 测试环境

- 仓库路径：`D:\1work\提示词管理`
- Shell：PowerShell
- Node 测试框架：`node --test`

## 测试命令

```powershell
node --test tests/unit/scripts/cleanup-real-db-sample-prompts.test.mjs
```

## 测试结果摘要

- 结果：通过
- 通过用例：4
- 失败用例：0

覆盖点：

1. `--dry-run` 只输出待删摘要，不执行删除
2. `--confirm` 缺少 `--operator` / `--reason` 时拒绝执行
3. `--confirm` 按事务顺序删除 `submissions -> current_version_id 置空 -> prompts 删除 -> audit`
4. 输入只接受显式 `slug` 清单

## 本次实现产物

- 脚本：`scripts/cleanup-real-db-sample-prompts.mjs`
- 测试：`tests/unit/scripts/cleanup-real-db-sample-prompts.test.mjs`
- 执行说明：`docs/2026-05-01-真实库样例清理执行说明.md`

## 真实库执行情况

- 本次会话未直接对真实库执行 `--confirm`
- 原因：当前未附带实际待清理 `slug` 清单，且任务重点为脚本、测试与文档落地

建议执行步骤：

1. 准备 `slug` 清单文件
2. 执行 `--dry-run`
3. 人工核对摘要
4. 执行 `--confirm --operator ... --reason ...`
5. 将实际命令、时间、输出摘要补记到本文件

## 待补充的执行留痕模板

```md
### 实际清理记录

- 执行时间：
- 执行人：
- 删除原因：
- 输入文件：
- dry-run 命令：
- dry-run 摘要：
- confirm 命令：
- confirm 结果：
- 备注：
```
