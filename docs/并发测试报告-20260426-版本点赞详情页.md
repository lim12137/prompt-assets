# 并发测试报告（版本点赞详情页）

## 执行时间
- 2026-04-26

## 测试命令
```bash
pnpm exec playwright test tests/e2e/smoke/prompt-detail-like.spec.ts tests/e2e/smoke/prompt-detail-copy.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
```

## 结果摘要
- 首轮：`prompt-detail-like`、`prompt-detail-candidate-flow` 失败，`prompt-detail-copy` 通过。
- 失败原因：
  - 初始点赞断言使用固定值，和当前环境初始渲染值不一致。
  - 详情卡片未完成版本级点赞接线（已在本次修复）。
- 修复后复跑：3/3 全部通过。

## 通过明细
- `tests/e2e/smoke/prompt-detail-like.spec.ts`：通过
- `tests/e2e/smoke/prompt-detail-copy.spec.ts`：通过
- `tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts`：通过

## 路由收敛复测（同日追加）
### 变更摘要
- 前端版本点赞请求由
  - `/api/prompts/{slug}/like?versionNo={versionNo}`
- 收敛为
  - `/api/prompts/{slug}/versions/{versionNo}/like`
- 同步更新 e2e mock 路由匹配与 `versionNo` 解析方式（由 query 改为 pathname）。

### 复测命令
```bash
pnpm exec playwright test tests/e2e/smoke/prompt-detail-like.spec.ts tests/e2e/smoke/prompt-detail-copy.spec.ts tests/e2e/smoke/prompt-detail-candidate-flow.spec.ts
```

### 复测结果
- `3 passed`
- `prompt-detail-like.spec.ts`：通过
- `prompt-detail-copy.spec.ts`：通过
- `prompt-detail-candidate-flow.spec.ts`：通过
