# 并发测试报告 - 2026-05-17 - 首页 AI 工具补充入口（最新验收）

## 验收前核对

- 已重读：
  - `apps/web/app/_home/home-ai-tools-section.jsx`
  - `tests/e2e/smoke/home-ai-tools.spec.ts`
- 发现并处理的轻微偏差：
  - 原测试通过标题祖先节点定位工具区，实际会落到 header 容器导致无法定位工具链接。
  - 已最小修正为使用 `data-testid="home-ai-tools-section"` 定位模块容器。

## 执行命令与最新结果

1. 命令

```bash
pnpm exec playwright test tests/e2e/smoke/home-ai-tools.spec.ts
```

结果摘要：
- 先失败 1 次（定位偏差：`CEIC Chat` 链接未命中）。
- 修正测试定位后重跑通过：`1 passed`。

2. 命令

```bash
pnpm exec playwright test tests/e2e/smoke/home.spec.ts --grep "home page task4: 页面名称为公司提示词库"
```

结果摘要：
- 通过：`1 passed`。

## 结论

- 基于当前代码状态，首页 AI 工具相关 smoke 与首页基础 smoke（指定子用例）均已通过。

