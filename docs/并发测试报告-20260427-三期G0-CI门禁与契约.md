# 并发测试报告-20260427-三期G0-CI门禁与契约

## 1. 范围

- 目标：验证三期 G0 新增 CI 门禁与评分契约文档的最小可用性。
- 分支目录：`D:\1work\提示词管理`

## 2. 执行命令

1. YAML 语法解析（新 workflow）

```bash
python -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('.github/workflows/phase3-g0-gate.yml').read_text(encoding='utf-8')); print('YAML_OK')"
```

2. 关键脚本存在性检查

```bash
pnpm run | rg -n "build:web|test:integration:critical|test:e2e:critical"
```

3. 关键 e2e 命令可执行性（发现测试用例）

```bash
pnpm run test:e2e:critical -- --list
```

4. 关键 integration 实跑

```bash
pnpm run test:integration:critical
```

## 3. 结果摘要

- YAML 解析：通过（输出 `YAML_OK`）。
- 脚本存在性：通过（`build:web` / `test:integration:critical` / `test:e2e:critical` 均可见）。
- e2e 命令校验：通过（发现 `prompt-detail-ux-candidate-like.spec.ts` 1 条测试）。
- integration 实跑：通过（`18 passed, 0 failed`）。

## 4. 备注

- `test:integration:critical` 运行中出现 Node `--experimental-strip-types` 与 `MODULE_TYPELESS_PACKAGE_JSON` 警告，不影响本次门禁结果。
- 本次未在本地执行完整 e2e 浏览器回放，采用 `--list` 完成命令可执行性与用例绑定校验；完整执行由 CI job `e2e:critical` 落地。
