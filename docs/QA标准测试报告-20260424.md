# QA 标准档测试报告（standard）

## 1. 测试日期

- 2026-04-24

## 2. 测试环境

- 仓库路径：`D:\1work\提示词管理`
- Node.js：使用仓库现有本地环境
- 应用运行方式：
  - Playwright 自动拉起：`http://127.0.0.1:3010`
  - 并发测试临时拉起：`http://127.0.0.1:3010`
- 备注：
  - 机器上 `http://127.0.0.1:3000` 当前是非本仓库的 Vite 服务，`/api/health` 返回 404（环境端口冲突）
  - k6 本地二进制不可用，且 Docker 拉取 `grafana/k6` 镜像失败（网络阻塞），并发改用 Node 本地并发脚本执行同场景

## 3. 测试范围（主链路）

- 首页浏览
- 分类/关键词筛选
- 详情页访问
- 复制当前版本
- 点赞 API
- 投稿 API
- 管理员审核 API（通过/拒绝）
- 健康检查 `/api/health`
- 并发：搜索 + 投稿

## 4. 验证命令与结果摘要

### 4.1 API 集成链路

```powershell
node --test --experimental-strip-types tests/integration/api/health.test.ts tests/integration/api/prompts-list.test.ts tests/integration/api/prompt-detail.test.ts tests/integration/api/prompt-like.test.ts tests/integration/api/prompt-submission.test.ts tests/integration/api/admin-submission-review.test.ts tests/integration/api/audit-log.test.ts
```

- 结果：`21 passed, 0 failed`
- 覆盖：`/api/health`、列表、详情、点赞、投稿、审核、审计日志

### 4.2 E2E / UI（Playwright）

```powershell
pnpm exec playwright test tests/e2e/smoke/home.spec.ts tests/e2e/smoke/prompt-detail-copy.spec.ts tests/e2e/ui/visual-baseline.spec.ts
```

- 结果：`6 passed, 0 failed`
- 覆盖：首页可见性、筛选/搜索、详情复制成功反馈、视觉基线

### 4.3 健康检查（环境核验）

```powershell
(Invoke-WebRequest -Uri http://127.0.0.1:3000/api/health -TimeoutSec 10).Content
```

- 结果：返回 `{"error":"Not found"}`（非本仓库服务）

```powershell
# 临时启动本仓库 web 到 3010 后检查
Invoke-WebRequest -Uri http://127.0.0.1:3010/api/health
```

- 结果：`200`，响应体 `{"status":"ok"}`

### 4.4 并发测试（搜索 + 投稿）

```powershell
# 尝试（失败，网络阻塞）
docker run --rm -i -e BASE_URL=http://host.docker.internal:3010 grafana/k6 run - < tests\concurrency\search-and-submit.js
```

- 结果：镜像拉取失败，无法执行

```powershell
# 替代方案：临时启动 3010 后，执行 Node 本地并发脚本（20 搜索 VU + 10 投稿 VU，12 轮）
# 场景等价于 tests/concurrency/search-and-submit.js 的核心路径
```

- 并发参数：
  - `rounds=12`
  - `searchVus=20`（总请求 `240`）
  - `submitVus=10`（总请求 `120`）
- 结果摘要：
  - 搜索成功率：`100% (240/240)`
  - 投稿成功率：`100% (120/120)`
  - 失败请求：`0`
  - 重复版本号：`0`

## 5. 问题清单（按严重级）

1. `Medium` 环境端口冲突：`127.0.0.1:3000` 非本仓库服务，导致该端口下 `/api/health` 返回 404。
   - 影响：若 QA 误用 3000，会得到假阴性结果。
   - 当前处理：本次测试统一使用临时拉起的 `127.0.0.1:3010` 完成验证。
   - 状态：`延后`（需由环境/启动编排侧统一端口策略）。

## 6. 修复与回归

- 本次代码缺陷修复数：`0`
- 本次文档落盘：`1`（本报告）
- 回归结论：在本仓库实例（3010）上，主链路与并发场景均通过

## 7. Health Score

- `95/100`
- 计算依据：
  - 功能与主链路通过率：满分
  - 并发稳定性：满分
  - 环境一致性扣分：`-5`（3000 端口冲突风险）

## 8. 最终结论

- QA 档位：`standard`
- 判定：`通过（带环境风险提示）`
